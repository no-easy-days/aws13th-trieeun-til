# [2026-01-06]

## ☀️ 오늘의 목표 (Scrum)

> 아침 스크럼이나 오늘 꼭 달성하고 싶은 목표를 적습니다.
>
- [ ]  3주차 과제 정리
- [ ]  파이썬 개인 공부

## 📝 배운 내용 (Today I Learned)

### 1. 엔드포인트 개선
분리된 Posts 엔드포인트
1. 게시글 목록 조회 - GET /posts

- 기능: 페이지네이션만 적용
- Query Parameters: page, limit

2. 게시글 검색 - GET /posts/search

- 기능: 제목/내용으로 검색
- Query Parameters: q (필수), page, limit

3. 게시글 정렬 조회 - GET /posts/sorted

- 기능: 최신순, 조회수순, 좋아요순 정렬
- Query Parameters: sort_by (필수: latest/views/likes), page, limit

개선된 Posts 엔드포인트
1. 게시글 목록 조회 - GET /posts (통합됨 ✅)

- Query Parameters: page, limit, search, sort
- 기능: 페이지네이션, 검색, 정렬 모두 지원
- 예시:

  - 기본 목록: GET /posts
  - 검색: GET /posts?search=클라우드
  - 정렬: GET /posts?sort=views

1.  엔드포인트 통합: /posts/search, /posts/sorted 삭제
2.  단일 엔드포인트로 통합: GET /posts에 모든 기능 포함
3.  RESTful 설계: 리소스 중심, Query Parameters로 옵션 제공
4.  일관성: 모든 목록 조회 엔드포인트에서 동일한 파라미터 사용

### 2. comments영역에서 post_id의 사용
현재 설계: PATCH /comments/{comment_id}
- comment_id만으로 댓글을 특정할 수 있음
- 서버에서 comment_id로 댓글을 찾으면 어느 게시글의 댓글인지 알 수 있음


> PATCH /posts/{post_id}/comments/{comment_id}
위와 같은 방법으로 설계하지 않는 이유?

1. comment_id는 전역적으로 고유함 - 모든 댓글은 고유한 ID를 가지므로 comment_id만으로 충분히 특정 가능
2. RESTful 원칙 - 리소스를 직접 식별할 수 있다면 더 짧은 경로 사용
3. 일관성 - 삭제도 DELETE /comments/{comment_id}로 설계되어 있음

## 💭 오늘의 회고 (Retrospective)

- **Keep (좋았던 점)**: API 명세를 직접 작성하면서 RESTful API에 대해 이해할 수 있었다.
- **Try (시도할 점)**:  RESTful 원칙에 따른 엔드포인트 설계 방법을 추가적으로 공부해야겠다.
