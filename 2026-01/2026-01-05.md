# [2026-01-05] FastAPI 학습을 위한 네트워크 및 HTTP 기초

> 강의 자료: Computer Networking: A Top-Down Approach (8th edition), notion 강의 자료
> 
> Chapter 2: Application Layer  
> Chapter 4: Network Layer

---

## 1. 웹의 동작 원리 (Web Mechanics)

### 1.1 클라이언트-서버 (Client-Server) 구조

웹은 기본적으로 **클라이언트-서버 패러다임**을 따름

#### **서버 (Server)**
- **Always-on host**: 항상 켜져 있는 호스트
- **Permanent IP address**: 고정된 IP 주소를 가짐
- **Data centers for scaling**: 확장을 위해 주로 데이터 센터에 위치
- 클라이언트의 요청을 기다리는 역할

#### **클라이언트 (Client)**
- 서버와의 통신을 **시작(initiate)**하는 주체
- **Intermittently connected**: 간헐적으로 연결될 수 있음
- **Dynamic IP addresses**: 유동 IP 주소를 가질 수 있음
- 서버에 요청을 보내고 응답을 받는 역할

**FastAPI와의 연결**  
FastAPI로 작성하는 애플리케이션은 바로 이 **'서버'** 역할을 수행하며, 특정 포트에서 클라이언트의 연결 요청을 기다리게 됨

---

### 1.2 프로세스 간 통신 (Process Communication)

#### **프로세스 (Process)**
- 호스트 내에서 실행 중인 프로그램
- 같은 호스트 내 프로세스 간 통신: **Inter-Process Communication (IPC)**
- 다른 호스트의 프로세스 간 통신: **메시지 교환**

#### **소켓 (Socket)**
- 프로세스가 메시지를 주고받는 **문(door)** 역할
- 애플리케이션 계층과 전송 계층 사이의 인터페이스
- 각 통신에는 **양쪽에 소켓이 하나씩** 필요

```
Application Layer  [controlled by app developer]
    ↕ [socket]
Transport Layer    [controlled by OS]
    ↕
Network Layer
```

---

### 1.3 주소 체계: IP, Port, DNS

#### **IP 주소 (IP Address)**
- 인터넷상의 호스트를 식별하는 고유 주소
- **IPv4**: 32비트 주소 (예: `128.119.245.12`)
- **IPv6**: 128비트 주소
- 데이터그램이 올바른 목적지 호스트로 전달되도록 함

#### **포트 번호 (Port Number)**
- 특정 호스트 내에서 **특정 프로세스(애플리케이션)를 식별**하는 번호
- 16비트 숫자 (0-65535)
- **Well-known ports** (표준 포트):
  - HTTP server: **80**
  - HTTPS server: **443**
  - Mail server (SMTP): **25**

#### **예시: HTTP 메시지 전송**
```
대상 주소: gaia.cs.umass.edu
→ IP address: 128.119.245.12
→ Port number: 80 (HTTP)
```

#### **DNS (Domain Name System)**
- 사람이 기억하기 쉬운 **호스트 이름**을 IP 주소로 변환
- **분산 데이터베이스** 시스템
- 계층적 구조로 구성

**변환 예시**
```
www.google.com  →  DNS  →  172.217.175.68
```

**DNS 서비스**
- Hostname-to-IP-address translation
- Host aliasing (별칭 지원)
- Mail server aliasing
- Load distribution (부하 분산)

---

## 2. HTTP 프로토콜 (HTTP Protocol)

### 2.1 HTTP 개요

**HTTP (HyperText Transfer Protocol)**
- 웹의 **애플리케이션 계층 프로토콜**
- 클라이언트와 서버가 메시지를 주고받는 규칙을 정의

#### **HTTP의 특징**
1. **Client/Server Model**
   - Client: 브라우저 (requests, receives, displays)
   - Server: 웹 서버 (sends objects in response)

2. **Uses TCP**
   - Client가 TCP 연결을 시작 (port 80)
   - Server가 TCP 연결을 수락
   - HTTP 메시지 교환
   - TCP 연결 종료
   - 비연결성 : 한 번 요청 후 응답이 끝나면 연결을 끊음

3. **Stateless Protocol**
   - 서버는 과거 클라이언트 요청에 대한 정보를 저장하지 않음
   - 각 요청은 독립적으로 처리됨
   - 상태 유지 필요 시 **쿠키(Cookies)** 사용

---

### 2.2 Request/Response 구조

#### **웹 페이지의 구조**
- **Base HTML file** + 여러 개의 **referenced objects**
- 각 객체는 **URL**로 주소 지정
  ```
  www.someschool.edu/someDept/pic.gif
  ├─ host name: www.someschool.edu
  └─ path name: /someDept/pic.gif
  ```

#### **HTTP 동작 방식**
1. 클라이언트가 **요청(Request)** 메시지 전송
2. 서버가 객체(HTML, 이미지, JSON 등)를 포함한 **응답(Response)** 메시지 전송

**FastAPI와의 연결**  
FastAPI는 이러한 HTTP 요청을 받아 처리하고, 적절한 응답을 만들어내는 도구

---

### 2.3 HTTP 메시지 구조

#### **HTTP Request 메시지**
```
GET /index.html HTTP/1.1\r\n                    ← Request line
Host: www-net.cs.umass.edu\r\n                  ┐
User-Agent: Mozilla/5.0 ...\r\n                 │
Accept: text/html,application/xhtml+xml\r\n     ├─ Header lines
Accept-Language: en-us,en;q=0.5\r\n             │
Accept-Encoding: gzip,deflate\r\n               │
Connection: keep-alive\r\n                      ┘
\r\n                                              ← Blank line
[Entity Body]                                    ← Body (optional)
```

**구성 요소**
- **Request line**: Method + URL + HTTP version
- **Header lines**: 메타데이터 (브라우저 정보, 콘텐츠 타입, 쿠키 등)
- **Blank line**: 헤더 종료를 나타냄 (`\r\n`)
- **Entity Body**: 실제 전송 데이터 (POST 요청에서 주로 사용)

#### **HTTP Response 메시지**
```
HTTP/1.1 200 OK\r\n                              ← Status line
Date: Tue, 08 Sep 2020 00:53:20 GMT\r\n         ┐
Server: Apache/2.4.6 (CentOS) ...\r\n           │
Last-Modified: Tue, 01 Mar 2016 18:57:50 GMT\r\n├─ Header lines
Content-Length: 2651\r\n                        │
Content-Type: text/html; charset=UTF-8\r\n      ┘
\r\n                                              ← Blank line
<html>...</html>                                 ← Entity Body
```

**구성 요소**
- **Status line**: Protocol version + Status code + Status phrase
- **Header lines**: 응답 메타데이터
- **Entity Body**: 실제 응답 데이터 (HTML, JSON, 이미지 등)

---

### 2.4 HTTP 연결 타입

#### **Non-persistent HTTP (HTTP/1.0)**
- 하나의 TCP 연결에서 **최대 1개의 객체** 전송
- 객체마다 새로운 TCP 연결 필요
- Response time = **2RTT + file transmission time**

**문제점**
- RTT가 객체마다 발생
- OS의 TCP 연결 오버헤드
- 병렬 TCP 연결 필요

#### **Persistent HTTP (HTTP/1.1)**
- 하나의 TCP 연결에서 **여러 객체** 전송 가능
- 서버가 응답 후에도 연결 유지
- Response time 절반으로 감소
- 현재 웹의 기본 방식

#### **HTTP/2 (2015)**
- 객체 전송 순서 우선순위 지정 가능
- **Frame** 단위로 객체 분할
- **HOL (Head-of-Line) blocking** 완화
- Server push 기능

#### **HTTP/3**
- **UDP** 기반 (QUIC 프로토콜)
- 객체별 독립적인 에러 및 혼잡 제어
- 보안 강화

---

## 3. HTTP Method & Status Code

### 3.1 HTTP Methods

클라이언트가 서버에게 **어떤 동작**을 원하는지 명시함

#### **GET**
- 서버로부터 **데이터를 요청**할 때 사용
- Body가 없음
- URL에 데이터를 포함할 수 있음 (query string)
  ```
  GET /search?q=python&lang=ko HTTP/1.1
  ```

#### **POST**
- 클라이언트가 데이터를 **서버로 전송**할 때 사용
- 주로 생성(Create) 작업
- Body에 데이터 포함
  ```
  POST /users HTTP/1.1
  Content-Type: application/json
  
  {"name": "John", "email": "john@example.com"}
  ```

#### **PUT**
- 지정된 URL의 리소스를 **완전히 대체**
- 업데이트(Update) 작업
- Body에 전체 데이터 포함

#### **HEAD**
- GET과 유사하지만 **Header만 요청**
- 객체는 반환하지 않음
- 리소스 존재 여부 확인에 사용

#### **DELETE**
- 지정된 리소스를 **삭제**

---

### 3.2 HTTP Status Codes

서버가 요청 처리 결과를 **숫자로 알려줌**.

#### **2xx: Success**
- **200 OK**
  - 요청이 성공적으로 처리됨
  - 요청한 객체가 메시지에 포함됨

#### **3xx: Redirection**
- **301 Moved Permanently**
  - 요청한 객체가 영구적으로 다른 곳으로 이동
  - `Location:` 헤더에 새 위치 포함

#### **4xx: Client Error**
- **400 Bad Request**
  - 서버가 요청을 이해할 수 없음
  - 클라이언트 측 오류

- **404 Not Found**
  - 요청한 문서(리소스)가 서버에 없음
  - 가장 흔한 에러 코드

#### **5xx: Server Error**
- **500 Internal Server Error**
  - 서버 내부 오류
  
- **505 HTTP Version Not Supported**
  - 서버가 요청된 HTTP 버전을 지원하지 않음

**💡 FastAPI에서의 활용**
```python
from fastapi import FastAPI, HTTPException

@app.get("/items/{item_id}")
async def read_item(item_id: int):
    if item_id not in items:
        raise HTTPException(status_code=404, detail="Item not found")
    return items[item_id]
```

---

## 4. REST API 개론

### 4.1 자원(Resource)과 행위(Verb)

#### **자원 (Resource)**
- 웹 페이지는 **객체(Object)**들로 구성
- 각 객체는 **URL**로 주소 지정됨
- REST API에서 URL은 '자원'을 나타냄

#### **행위 (Verb)**
- HTTP Method가 자원에 대한 '행위'를 나타냄
- GET: 조회 (Read)
- POST: 생성 (Create)
- PUT: 수정 (Update)
- DELETE: 삭제 (Delete)

**예시: URL 설계**
```
GET    /users          # 모든 사용자 조회
GET    /users/123      # 특정 사용자 조회
POST   /users          # 새 사용자 생성
PUT    /users/123      # 사용자 정보 수정
DELETE /users/123      # 사용자 삭제
```

---

### 4.2 Stateless (무상태성)

#### **HTTP는 Stateless 프로토콜**
- 서버는 클라이언트의 **과거 요청 정보를 저장하지 않음**
- 각 요청은 **독립적**으로 처리됨

**장점**
- 서버 확장(Scale-out)이 쉬움
- 서버 장애 시 복구가 간단
- 서버 부하 감소

**상태 유지가 필요한 경우**
- **Cookies**: 클라이언트 측 상태 저장
- **Session**: 서버 측 상태 저장 (세션 ID는 쿠키로 관리)
- **JWT (JSON Web Token)**: 인증 토큰

---

## 5. 데이터 포맷 (JSON)

### 5.1 HTTP에서의 데이터 전송

#### **Content-Type Header**
전송되는 데이터의 형식을 명시

**일반적인 MIME 타입**
- `text/html` - HTML 문서
- `text/plain` - 일반 텍스트
- `application/json` - JSON 데이터 (**FastAPI의 기본**)
- `application/xml` - XML 데이터
- `image/jpeg` - JPEG 이미지
- `multipart/form-data` - 파일 업로드

**예시: JSON 응답**
```
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 82

{"id": 123, "name": "John Doe", "email": "john@example.com"}
```

---

### 5.2 JSON (JavaScript Object Notation)

#### **JSON의 특징**
- 경량의 데이터 교환 형식
- 사람이 읽고 쓰기 쉬움
- 기계가 파싱하고 생성하기 쉬움
- 언어 독립적 (Language-independent)

#### **JSON 기본 구조**

**데이터 타입**
```json
{
  "string": "문자열",
  "number": 123,
  "float": 45.67,
  "boolean": true,
  "null": null,
  "array": [1, 2, 3],
  "object": {"key": "value"}
}
```

**중첩 구조**
```json
{
  "user": {
    "id": 1,
    "name": "Alice",
    "emails": ["alice@example.com", "alice@work.com"],
    "address": {
      "city": "Seoul",
      "country": "Korea"
    }
  }
}
```

---

### 5.3 Python에서의 JSON 처리

#### **직렬화 (Serialization)**
Python 객체 → JSON 문자열

```python
import json

# Dictionary to JSON string
data = {
    "name": "John",
    "age": 30,
    "city": "Seoul"
}

json_string = json.dumps(data)
# '{"name": "John", "age": 30, "city": "Seoul"}'
```

#### **역직렬화 (Deserialization)**
JSON 문자열 → Python 객체

```python
import json

json_string = '{"name": "John", "age": 30}'
data = json.loads(json_string)
# {'name': 'John', 'age': 30}
```

#### **FastAPI에서의 자동 처리**
FastAPI는 JSON 직렬화/역직렬화를 자동으로 처리함

```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class User(BaseModel):
    name: str
    email: str
    age: int

@app.post("/users/")
async def create_user(user: User):
    # FastAPI가 자동으로 JSON을 User 객체로 변환
    return user  # FastAPI가 자동으로 User를 JSON으로 변환
```

**요청 예시**
```bash
POST /users/ HTTP/1.1
Content-Type: application/json

{"name": "Alice", "email": "alice@example.com", "age": 25}
```

**응답 예시**
```bash
HTTP/1.1 200 OK
Content-Type: application/json

{"name": "Alice", "email": "alice@example.com", "age": 25}
```

---

## 💭 오늘의 회고 (Retrospective)

- **Keep (좋았던 점)**: 
  - 수업에서 배운 내용을 전공교재와 함께 공부하니까 이해가 더 잘 되었다.

- **Problem (아쉬웠던 점)**:
  - 배웠던 내용을 말로 설명할 수 있을 정도의 공부가 필요하다.

- **Try (시도할 점)**: 
  - 네트워크 부분의 추가 학습 필요
