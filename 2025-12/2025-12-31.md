# [2025-12-31] Python 선행학습

## ☀️ 오늘의 목표 (Scrum)

> 아침 스크럼이나 오늘 꼭 달성하고 싶은 목표를 적습니다.
>
- [ ]  1주차 깃&깃허브 과제
- [ ]  파이썬 수업 내용 배운 곳까지(1,2주차) 공부

## 📝 배운 내용 (Today I Learned)

### 변수와 자료형
- **기본 자료형**: int(정수), float(실수), str(문자열), bool(참/거짓)
- **컬렉션 자료형**: list(리스트), dict(딕셔너리), tuple(튜플), set(집합)
- 변수는 자료형을 명시하지 않아도 자동으로 할당됨
```python
# 기본 자료형
age = 25  # int
height = 175.5  # float
name = "Python"  # str
is_student = True  # bool

# 컬렉션 자료형
numbers = [1, 2, 3]  # list - 순서O, 중복O, 수정O
person = {"name": "Kim", "age": 20}  # dict - key:value 쌍
coordinates = (10, 20)  # tuple - 순서O, 수정X
unique_nums = {1, 2, 3}  # set - 순서X, 중복X
```

### 조건문과 반복문
- **if문**: 조건에 따라 코드 실행 여부 결정
- **for문**: 반복 가능한 객체를 순회
- **while문**: 조건이 참인 동안 반복 실행
```python
# 조건문
score = 85
if score >= 90:
    print("A")
elif score >= 80:
    print("B")
else:
    print("C")

# for문
for i in range(5):
    print(i)  # 0, 1, 2, 3, 4

# while문
count = 0
while count < 3:
    print(count)
    count += 1
```

### 함수 정의와 호출
- `def` 키워드로 함수 정의
- 매개변수와 반환값 사용 가능
- 함수명()으로 호출
```python
# 기본 함수
def greet(name):
    return f"Hello, {name}!"

result = greet("Python")
print(result)  # Hello, Python!

# 기본값이 있는 매개변수
def add(a, b=10):
    return a + b

print(add(5))  # 15
print(add(5, 3))  # 8
```

### 기본 입출력
- **input()**: 사용자로부터 입력 받기 (문자열로 반환)
- **print()**: 화면에 출력하기
```python
# 입력
name = input("이름을 입력하세요: ")
age = int(input("나이를 입력하세요: "))  # 숫자로 변환

# 출력
print("안녕하세요!")
print(f"이름: {name}, 나이: {age}")  # f-string 사용
print(name, age, sep=", ")  # 구분자 지정
```

### 클래스
- 객체 지향 프로그래밍의 기본 단위
- 속성(변수)과 메서드(함수)를 포함
- `__init__`: 생성자 메서드 (객체 초기화)
- `self`: 인스턴스 자신을 참조
```python
class Student:
    def __init__(self, name, age):
        self.name = name  # 속성
        self.age = age
    
    def introduce(self):  # 메서드
        return f"저는 {self.name}이고, {self.age}살입니다."

# 객체 생성 및 사용
student1 = Student("김철수", 20)
print(student1.introduce())  # 저는 김철수이고, 20살입니다.
```

## 💭 오늘의 회고 (Retrospective)

- **Keep (좋았던 점)**: 
  - 기본 자료형부터 클래스까지 체계적으로 학습할 수 있었음

- **Problem (아쉬웠던 점)**: 

- **Try (시도할 점)**: 
  - 클래스 상속과 예외 처리를 추가로 학습할 예정
